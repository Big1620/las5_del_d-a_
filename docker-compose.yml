# Las 5 del día - Docker Compose
# Uso: docker-compose up -d
# Variables: crear .env o .env.production con NEXT_PUBLIC_WP_API_URL, NEXT_PUBLIC_SITE_URL, etc.

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_WP_API_URL=${NEXT_PUBLIC_WP_API_URL}
        - NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL}
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health',(r)=>{process.exit(r.statusCode===200?0:1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
